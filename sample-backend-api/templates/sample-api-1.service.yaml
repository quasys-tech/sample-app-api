apiVersion: v1
kind: Service
metadata:
  name: '{{ .Release.Name }}-sample-api-1'
spec:
  selector:
    app: sample-api-1
    app/release: '{{ .Release.Name }}'
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
